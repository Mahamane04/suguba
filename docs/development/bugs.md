# Registre des Anomalies & Bugs — Suguba (V3.0)

| ID | Description | Sévérité | Statut | Résolution / Correctif |
| :--- | :--- | :--- | :--- | :--- |
| `BUG-001` | Lucide icon title prop type error dans `CloudSyncBadge.tsx` | Faible | **RÉSOLU** | Entouré l'icône d'un tag `<span>` avec attribut title valide HTML. |
| `BUG-002` | Type name mismatch `WithdrawalRequest` vs `Withdrawal` dans `cloud-sync.ts` | Moyen | **RÉSOLU** | Aligné l'import sur le type unifié `Withdrawal` défini dans `src/types/index.ts`. |
| `BUG-003` | Répétition accidentelle du bouton simulateur dans le header revendeur | Mineur | **RÉSOLU** | Remplacé par les boutons officiels `Studio Stories 9:16` et `Ma Carte Pro & QR`. |
